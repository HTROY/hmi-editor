# 报警与 SOE 由后端 Active 节点权威计算，前端仅展示

报警与 SOE 的计算从 HMI 编辑器前端迁移到 Rust IO 后端，只有 Active 节点执行判定与持久化，再经 WebSocket/REST 推送给前端；前端仅负责展示与确认操作。这样做的原因是多端一致性、掉电不丢历史、以及轨交场景对毫秒级时序和设备时间的依赖。编辑器在仿真模式（未连接后端）下保留同一语义的本地降级引擎，但界面必须标注“模拟”，避免与真实数据混淆。

后续修订：报警规则的编辑入口收敛到管理端（见 0002），HMI 编辑器在连接后端时不展示规则界面，仅仿真模式保留本地简易编辑器。
