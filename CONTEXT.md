# HMI Editor

轨道交通 HMI 配置与运行工具：编辑器负责画面/变量/脚本配置，采集后端负责从协议插件取数并向各端推送实时状态。

## Language

**报警规则（Alarm Rule）**:
定义某个点位在何种条件下产生报警的正式配置（条件、阈值、级别、分组、使能），全系统唯一事实来源。
_Avoid_: 报警定义、阈值字段

**管理端（Management UI）**:
由采集后端提供访问的 Web 操作界面，是报警规则编辑的唯一入口。
_Avoid_: 后端页面、web-ui（作为领域术语时）

**报警规则管理（Alarm Rule Management）**:
在管理端对报警规则进行新增、修改、启停、删除的操作；HMI 编辑器仅在仿真模式提供简化本地版本。
_Avoid_: 规则编辑（与具体表单混淆）

**报警事件（Alarm Event）**:
一次报警从触发到恢复的完整记录，携带触发、确认、恢复时间与操作员。
_Avoid_: 报警记录（与规则混淆时）

**活跃报警（Active Alarm）**:
尚未恢复的报警事件，无论是否已被确认。
_Avoid_: 当前报警

**确认（Acknowledge）**:
操作员对未确认报警作出的“已知悉并处理”标记，状态全系统唯一。
_Avoid_: 消音、复位

**SOE 记录（SOE Record）**:
点位值发生变化的顺序事件记录（毫秒精度、含质量），与报警事件分开建模。
_Avoid_: 事件日志、SOE 报警

**设备时间（Device Time）**:
点位事件由采集设备给出的毫秒时间戳，SOE 排序优先采用；设备未提供时以后端接收时间兜底。
_Avoid_: 时间戳（过于宽泛）

**报警发生记录（Alarm Occurrence）**:
一次报警从触发到恢复的摘要行，聚合触发、确认、恢复等明细事件。
_Avoid_: 报警汇总（容易与统计报表混淆）

**报警明细事件（Alarm Stream Event）**:
一次报警发生过程中触发、确认、恢复等状态变化的独立事件行，归属于某个报警发生记录。
_Avoid_: 报警流水（歧义）

**瞬时报警（Transient Alarm）**:
“变位（change）”条件产生的一次性报警事件，记录后立即结束，不常驻活跃表。
_Avoid_: 瞬时事件

**质量保持（Quality Hold）**:
点位质量非 good 时暂停阈值判定并保持原报警状态；质量恢复后再按新值重判。
_Avoid_: 质量抑制（易误解为删除报警）

**仿真降级（Simulation Fallback）**:
编辑器未连接采集后端时，在本地以同一套规则计算报警/SOE 的模式，展示上必须标明“模拟”。
_Avoid_: 离线模式（容易与断线重连混淆）

**Active 节点**:
冗余对中当前承担采集与推送职责的节点，报警与 SOE 计算只在其上发生。
_Avoid_: 主机、主节点（与实例级 primary 混淆）

### 画面编辑

**图元（Shape）**:
页面上可独立配置的最小图形对象（矩形/圆/直线/文本/专用图元/组），携带位置、尺寸、旋转、样式、绑定与动画。
_Avoid_: 图形、元素（与通用 UI 术语混淆）

**页面（Page）**:
工程中的一个独立画面，拥有自己的页面分辨率、背景与图元集合，可切换编辑。
_Avoid_: 场景（与运行时画面概念混淆）

**页面分辨率（Page Resolution）**:
页面的逻辑尺寸（宽×高，像素），决定画面边界与导出结果，不受编辑器视图缩放影响。
_Avoid_: 分辨率（单独使用时易与屏幕分辨率混淆）

**视图缩放（View Zoom）**:
编辑器对页面的显示缩放与平移，只影响编辑视图，不改写图元坐标与页面分辨率。
_Avoid_: 缩放（与图元缩放、页面分辨率混用）

**组（Group）**:
一组图元的整体容器，SVG 导入的默认产物；整体缩放时子图元保持相对布局。
_Avoid_: 组合（与成组操作混淆）

**栅格图元（Image Shape）**:
由 PNG/JPG 等位图导入的图元，图片数据随工程持久化。
_Avoid_: 图片（与形状图元混淆时）

**变量绑定（Binding）**:
将变量值经映射（直接/枚举/范围/状态色/位掩码）写入图元属性的配置。
_Avoid_: 数据绑定（过于宽泛）

**动画（Animation）**:
图元随时间发生的连续视觉变化（闪烁/旋转/位移/缩放/变色），可由变量驱动，数值属性可带平滑过渡。
_Avoid_: 动效（营销用语）

**平滑过渡（Smooth Transition）**:
绑定数值属性从旧值到新值的插值过程，默认约 300ms，避免视觉跳变。
_Avoid_: 动画插值（与关键帧概念混淆）

**工程（Project）**:
一组页面及共享配置的集合，可整体保存、自动保存、导入导出。
_Avoid_: 项目（过于宽泛）

**自动保存（Autosave）**:
编辑停止后自动把工程快照写入本地持久化，页面切换或刷新后无感恢复。
_Avoid_: 保存（与显式导出混淆）

**工程同步（Project Sync）**:
将本地自动保存的工程快照显式推送到 io-backend 或从 io-backend 拉取，使另一台设备能继续编辑同一工程。
_Avoid_: 云端同步、备份

**工程包（Project Package）**:
以 `.hmi.zip` 形式存在的跨设备交换与后端传输格式，内含 manifest.json 与 assets/ 资源目录。
_Avoid_: 导出文件、zip 文件（过于宽泛）
